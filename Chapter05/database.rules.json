{
  "rules": {
    "helpdesk": {
      "tickets": {
        "$ticket_id": {
          ".read": "auth != null && data.child('email').val() == auth.email",
          ".write": "auth != null"
        }
      }
    }
  }
}
